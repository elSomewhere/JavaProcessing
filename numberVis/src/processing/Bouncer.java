package processing;

import controlP5.Controller;

public class Bouncer {
	//method for increasing a parameter by x, only called after frequency peak
	//method for decreasing a parameter step by step (either to start value or to...?), called in every other draw frame
	
	public void bounceUp(float dampen){
		
	}
	
	
	public void fallBack(Controller<?> in, float dampen){
		//in.setValue(in.getValue()-dampen*startValues.get(in.getName()).getValue());
	}
	
}
